import React from "react";
import { Box, Typography } from "@mui/material";

function Subdomain() {
    return (
        <Box sx={{ color: "#fff" }}>
            <Typography variant="h2">Subdomain Enumeration</Typography>
        </Box>
    );
}

export default Subdomain;