import React from "react";
import { Box, Typography } from "@mui/material";

function IPandPorts() {
    return (
        <Box sx={{ color: "#fff" }}>
            <Typography variant="h2">IP and Ports</Typography>
        </Box>
    );
}

export default IPandPorts;